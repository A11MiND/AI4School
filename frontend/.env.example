# Frontend browser API base URL
# Local dev typically keeps /api and relies on Next rewrite.
NEXT_PUBLIC_API_BASE_URL=/api

# Next.js server-side rewrite target
# Set this in deployment platform to your backend public URL.
API_PROXY_TARGET=http://127.0.0.1:8000
